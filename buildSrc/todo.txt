The publication module has publishing configured in its build.gradle file.
But writing so much code in each module isn't exactly chic.
It all needs to be moved to a separate GotoMaven plugin.
Create the plugin in the buildSrc directory.

Veriable `publicationModules` of module names move to params of this plugin.

